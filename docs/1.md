
设置里面添加 CPA 服务表格，支持添加，操作列支持编辑、测试、删除， 添加 cpa 配置，支持设置名称、API URL、支持 API Token 等，字段参考 ./image.png

控制台的 Team 列表，就需要调整一下了，配置不再自己管理了，而是取用 CPA 服务里面的，文档在：https://help.router-for.me/cn/management/api.html

我自己梳理的流程是：
从 /management/auth-files 获取 "provider": "codex" 的账号，然后选择哪些账号是team 母号需要在这个系统里面管理的（根据邮箱 email），

获取具体的配置是根据 /management/auth-files 里面的 name 调用 /auth-files/download?name= 获取配置 